{% extends 'base.html.twig' %}

{% block title %}Edit OurMission{% endblock %}

{% block body %}   
<section class="add_new-form">
        <div class="container">    
                <div class="form_content">
                    <div class="form_title-box">
                        <div class="form_title">
                            Страница заполнения блока "Наша миссия" на странице о нас
                        </div>
                        <div class="form_text">
                            При заполнении формы новостной статьи,пожалуйста следуйте правилам публикации статей на сайте и подсказкам системы.
                        </div>
                    </div>
                    <div class="add_new-form form_box">
                    {{ include('our_mission/_form.html.twig', {'button_label': 'Update'}) }}
                    {# {{ include('our_mission/_delete_form.html.twig') }}   #}
                    </div>   
                </div>   
        </div>
    </section>
    <div id="hidden-content">
    {{ form_start(fast_consultation_form) }}
        {{ form_widget(fast_consultation_form) }}
        <input type="submit" value="Отправить">
    {{ form_end(fast_consultation_form) }}
</div>
{% endblock %}
