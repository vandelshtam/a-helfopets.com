{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>OurMission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ our_mission.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ our_mission.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ our_mission.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ our_mission.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('our_mission_index') }}">back to list</a>

    <a href="{{ path('our_mission_edit', {'id': our_mission.id}) }}">edit</a>

    {{ include('our_mission/_delete_form.html.twig') }}
{% endblock %}
