{% extends 'base.html.twig' %}

{% block title %}Achievements{% endblock %}

{% block body %}
    <h1>Achievements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ achievement.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ achievement.title }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ achievement.img }}</td>
            </tr>
            <tr>
                <th>Paragraph</th>
                <td>{{ achievement.paragraph }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ achievement.link }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ achievement.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('achievements_index') }}">back to list</a>

    <a href="{{ path('achievements_edit', {'id': achievement.id}) }}">edit</a>

    {{ include('achievements/_delete_form.html.twig') }}
{% endblock %}
