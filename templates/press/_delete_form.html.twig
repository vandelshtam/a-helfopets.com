<form method="post" action="{{ path('press_delete', {'id': press.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ press.id) }}">
    <button class="default-btn-about admin">Удалить</button>
</form>
