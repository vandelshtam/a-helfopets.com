{% extends 'base.html.twig' %}

{% block title %}Edit Blog{% endblock %}

{% block body %}
<section class="form add_blog">
        <div class="container">
                <div class="form_content">
                    <div class="form_title-box">
                        <div class="form_title">
                            Страница создания нового поста
                        </div>
                        <div class="form_text">
                            При создании поста пожалуйста следуйте рекомендациям системы
                        </div>
                    </div>
                    <div class="form_box">
                        {{ include('blog/_form.html.twig', {'button_label': 'Update'}) }}
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                        {{ include('blog/_delete_form.html.twig') }}
                        {% endif %}   
                    </div>   
                </div>    
        </div>
</section>
<div id="hidden-content">
    {{ form_start(fast_consultation_form) }}
        {{ form_widget(fast_consultation_form) }}
        <input type="submit" value="Отправить">
    {{ form_end(fast_consultation_form) }}
</div> 
{% endblock %}
{% block javascripts %}
    {{ include('js.html.twig') }}
{% endblock %}
