<form method="post" action="{{ path('fotoblog_delete', {'id': fotoblog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fotoblog.id) }}">
    <button class="default-btn blog">Удалить</button>
</form>
